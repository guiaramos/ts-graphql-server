type User {
  id: ID!
  email: String!
  isConfirmed: Boolean
}

type Query {
  hello(name: String): String!
}

type Success {
  message: String!
}

type Mutation {
  register(email: String!, password: String!): Success!
}
